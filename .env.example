# ─────────────────────────────────────────────────────────────
#  Sonna Patisserie & Cafe — .env.example
# ─────────────────────────────────────────────────────────────
# This is a template file. Copy it to '.env' and fill in the values.
# NEVER commit the real '.env' file to version control.

# Database (server-only)
# DATABASE_URL is a privileged credential. Keep it in .env.server.example, not here.
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_ID.supabase.co:5432/postgres"

# Supabase Keys
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY

# Aliases for compatibility
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY

# WhatsApp Business API (public-facing only)
# Note: WHATSAPP_VERIFY_TOKEN is a server-only secret - see .env.server.example
NEXT_PUBLIC_WHATSAPP_NUMBER=91...
NEXT_PUBLIC_WHATSAPP_NUMBER_FORMATTED=+91XXXXXXXXXX

# Razorpay Payments
RAZORPAY_KEY_ID=rzp_test_...
